import { DefaultContent as Content } from './Content';
import { DefaultNavigation as Navigation } from './Navigation';
import { DefaultProvider as Provider } from './Provider';

export const DefaultLayout = {
    Content,
    Navigation,
    Provider,
};